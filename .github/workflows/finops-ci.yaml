name: FinOps Cost Guardrail

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read
  pull-requests: write

env:
  COST_THRESHOLD_USD: "50"
  TF_IN_AUTOMATION: "true"

jobs:
  cost-guardrail:
    name: Terraform plan and Infracost guardrail
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.8.5

      - name: Setup Infracost
        uses: infracost/actions/setup@v3
        with:
          api-key: ${{ secrets.INFRACOST_API_KEY }}

      - name: Ensure jq is installed
        run: |
          if ! command -v jq >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y jq
          fi

          jq --version

      - name: Terraform init
        working-directory: infra
        run: terraform init

      - name: Terraform validate
        working-directory: infra
        run: terraform validate

      - name: Terraform plan
        working-directory: infra
        run: terraform plan -out tfplan.binary -var-file=envs/dev.tfvars

      - name: Export Terraform plan JSON
        working-directory: infra
        run: terraform show -json tfplan.binary > plan.json

      - name: Infracost breakdown JSON
        run: |
          # TODO: Use infracost diff --compare-to after baseline cost artifacts are available on main.
          infracost breakdown \
            --path . \
            --format json \
            --out-file infracost.json

      - name: Infracost breakdown table summary
        run: |
          infracost breakdown \
            --path . \
            --format table \
            --out-file infracost-table.txt

      - name: Comment Infracost summary on PR
        if: github.event_name == 'pull_request'
        run: |
          infracost comment github \
            --path infracost.json \
            --repo "${{ github.repository }}" \
            --pull-request "${{ github.event.pull_request.number }}" \
            --github-token "${{ github.token }}" \
            --behavior update

      - name: Enforce monthly cost threshold
        run: |
          estimated_monthly_cost="$(jq '[.projects[]?.breakdown.totalMonthlyCost // "0" | tonumber] | add // 0' infracost.json)"

          echo "Estimated monthly cost: \$${estimated_monthly_cost}"
          echo "Allowed estimated monthly cost: \$${COST_THRESHOLD_USD}"

          awk -v cost="${estimated_monthly_cost}" -v threshold="${COST_THRESHOLD_USD}" '
            BEGIN {
              if (cost > threshold) {
                printf "Cost guardrail failed: estimated monthly cost %.2f exceeds threshold %.2f\n", cost, threshold
                exit 1
              }

              printf "Cost guardrail passed: estimated monthly cost %.2f is within threshold %.2f\n", cost, threshold
            }
          '
